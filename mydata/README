This directory contains the result for the project:
lm_train_dir contains all the training data (train_dir + trial_dir from the data folder);
lm_train_result contains training result following the task required format for each task along with language model scores.
plain.txt is the training data file used by the language model;
text.arpa is the language model generated by kenlm.
