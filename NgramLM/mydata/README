This directory contains the result for the project:
lm_train_dir contains all the training data (train_dir + trial_dir from the data folder);
lm_train_result contains training result following the task required format for each task along with language model scores.
For the tweets training data:
plain.txt is the tweets training data file used by the language model.
text.arpa is the tweets language model generated by kenlm.
For the news training data:
You'll have to collect the training data yourself. Steps please see README under script folder.
